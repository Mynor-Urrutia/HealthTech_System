# ============================================================
# HealthTech - Plantilla de Variables de Entorno
# ============================================================
# Copie este archivo como .env y complete con sus valores reales.
# cp .env.example .env
# ============================================================

# Django
DJANGO_SECRET_KEY=GENERAR-CON: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
DJANGO_DEBUG=False
DJANGO_ALLOWED_HOSTS=su-dominio.com,www.su-dominio.com

# Base de Datos Oracle
DB_ENGINE=django.db.backends.oracle
DB_NAME=XE
DB_USER=healthtech
DB_PASSWORD=SU_PASSWORD_SEGURA
DB_HOST=localhost
DB_PORT=1521

# CORS (dominios permitidos separados por coma)
CORS_ALLOWED_ORIGINS=https://su-dominio.com

# JWT
JWT_ACCESS_TOKEN_LIFETIME_MINUTES=15
JWT_REFRESH_TOKEN_LIFETIME_DAYS=1
